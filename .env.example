# 🔒 ENVIRONMENT CONFIGURATION
# Copy this file to .env and set your actual values
# NEVER commit the actual .env file to version control

# 🚨 CRITICAL SECURITY SETTINGS
# Generate a strong random secret key for admin API access
# Use: openssl rand -hex 32 or similar tool
ADMIN_SECRET_KEY=your_secure_admin_secret_key_here_32_chars_minimum

# Database Configuration (Railway provides these automatically)
DATABASE_URL=postgresql://username:password@host:port/database

# Server Configuration
PORT=3000
NODE_ENV=development

# Security Headers
CORS_ORIGINS=https://aistudio555.com,https://www.aistudio555.com

# JWT Configuration (for future authentication improvements)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=24h

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info